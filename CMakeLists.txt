# this is the build file for project VulkanApp
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.15.0)
cmake_policy(SET CMP0091 NEW)
project(VulkanApp VERSION 0.1.0 LANGUAGES CXX)
set(CURRENT_COMPILER_ID ${CMAKE_CXX_COMPILER_ID})

# target
add_executable(vulkan_app "")
set_target_properties(vulkan_app PROPERTIES OUTPUT_NAME "vulkan_app")
set_target_properties(vulkan_app PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/bin")
target_include_directories(vulkan_app PRIVATE
    C:/VulkanSDK/1.4.335.0/Include
)
target_include_directories(vulkan_app SYSTEM PRIVATE
    C:/Users/Gerard/AppData/Local/.xmake/packages/g/glfw/3.4/d59f6dd71e7647cc95d2c1d35e51a39a/include
    C:/Users/Gerard/AppData/Local/.xmake/packages/g/glm/1.0.2/bbb3ed25088240d78f511829f81ea1ca/include
)
target_compile_definitions(vulkan_app PRIVATE
    GLFW_INCLUDE_NONE
)
target_compile_options(vulkan_app PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CUDA>:-DNDEBUG>
)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(vulkan_app PRIVATE /EHsc)
elseif(CURRENT_COMPILER_ID STREQUAL "Clang")
    target_compile_options(vulkan_app PRIVATE -fexceptions)
    target_compile_options(vulkan_app PRIVATE -fcxx-exceptions)
elseif(CURRENT_COMPILER_ID STREQUAL "GNU")
    target_compile_options(vulkan_app PRIVATE -fexceptions)
endif()
set_target_properties(vulkan_app PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(vulkan_app PRIVATE cxx_std_20)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(vulkan_app PRIVATE $<$<CONFIG:Release>:-O2>)
else()
    target_compile_options(vulkan_app PRIVATE -O3)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
else()
    target_compile_options(vulkan_app PRIVATE -fvisibility=hidden)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    set_property(TARGET vulkan_app PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
endif()
target_link_libraries(vulkan_app PRIVATE
    vulkan-1
    gdi32
    glfw3
    opengl32
    user32
    shell32
    gdi32
)
target_link_directories(vulkan_app PRIVATE
    C:/VulkanSDK/1.4.335.0/Lib
    C:/Users/Gerard/AppData/Local/.xmake/packages/g/glfw/3.4/d59f6dd71e7647cc95d2c1d35e51a39a/lib
)
# private sourcefiles from sourcebatch c++.build for target vulkan_app
target_sources(vulkan_app PRIVATE 
    src/main.cpp
)

